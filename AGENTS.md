# 项目协作说明

## 开始工作前

1. 阅读 `README.md`、`PROJECT_STATUS.md` 和 `CROSS_DEVICE_SETUP.md`。
2. 先检查工作区及远端状态，保留用户已有的未提交修改。
3. 修改前确认当前实现和测试，不把路线图中的设想当成已完成功能。

## 产品定位

这是一个面向个人研究的 A 股策略实验室。近期目标是完成数据获取、策略回测、
自选股信号和通知闭环，交易最终由用户人工确认。项目不提供投资建议，也不应在
没有独立风控和明确授权的情况下自动下单。

## 工程约定

- 前端使用 Streamlit，图表使用 Plotly，行情入口集中在 `market_data.py`。
- 策略计算集中在 `quant_core.py`，不要把核心回测逻辑散落到页面代码中。
- 行情源必须允许失败和回退。目前顺序为东方财富、腾讯、新浪、磁盘缓存。
- 新策略应使用统一输入输出约定，并为信号、持仓、费用和指标增加测试。
- 严禁未来数据。收盘后生成的信号最早从下一交易日收益开始计入回测持仓。
- 涉及交易时段、交易所规则和税费时，先核对最新官方规则，并在界面说明适用范围。
- 保持密钥、令牌、缓存、日志和本地环境文件不进入 Git。
- 完成修改后至少运行 `python -m pytest -q`。

## GitHub 协作

- 主仓库：`OverHabit/szy`
- 默认分支：`master`
- 用户明确要求“上传”时，检查改动和测试后直接提交到 GitHub，并验证远端文件。
- 提交信息应描述用户可见的变化；不要提交 `.cache/`、`.venv/`、日志或令牌。
- 从其他电脑开始工作前先拉取远端变更，发现冲突时不要覆盖另一台电脑的修改。
- 删除仓库、强制推送、改变可见性或扩大权限前，必须获得用户明确确认。

## 文档维护

每完成一个阶段，更新 `PROJECT_STATUS.md`：

- 将已交付功能移入“当前已完成”。
- 记录关键设计决定和已知限制。
- 调整下一阶段顺序，但保留尚未完成事项的真实状态。
