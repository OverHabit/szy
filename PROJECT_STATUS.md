# A股策略实验室：开发思路与进度

更新日期：2026-08-06

## 产品目标

逐步做成一个可上云的个人量化研究与信号助手：

1. 获取可靠的 A 股日线数据。
2. 同一股票或股票池可选择、比较多种策略。
3. 通过截图识别股票名称或代码，一次加入多只股票。
4. 每个交易日收盘后自动更新数据、运行策略并发送信号提醒。
5. 用户核对信号和交易规则后人工执行交易。

近期不做自动下单。先保证数据、回测、信号时点和通知链路可信，再讨论券商接口及
更严格的实盘风控。

## 当前已完成

### 本地网页

- Streamlit 本地应用，默认地址为 `http://localhost:8501`。
- 支持输入 6 位股票代码或完整股票名称。
- 支持选择回测日期、复权方式、长短均线和单边费用。
- 展示 K 线、均线、买卖点、策略净值、基准净值、回撤、指标和交易记录。
- 回测指标采用两行自适应布局，避免在侧栏展开时截断数值；图表默认聚焦最近一年，
  并提供 3 个月、6 个月、1 年、3 年与全部历史的快捷查看范围。
- 支持在页面选择双均线趋势或放量突破策略；成交量以独立子图展示，且可展开查看
  每种策略的规则、适用环境和主要风险，所有回测参数均提供悬浮与汇总说明。
- 回测概览的收益、回撤、夏普比率、交易次数和胜率等指标均提供计算口径说明。
- 已增加“自选信号”页面：可在网页内增加、编辑、暂停和删除股票或 ETF 的独立策略
  配置，初始已加入科创50ETF（`588000`）双均线配置；配置落在本机 SQLite 数据库，
  不需要维护 JSON 文件。
- 已增加 `daily_signal.py`：读取启用的本机自选配置，校验当日日线是否更新，输出含
  股票、策略、参考收盘价、原因和在线网页链接的邮件正文；不含邮箱密码或自动下单功能。

### 策略与回测

- 首个策略为双均线趋势策略。
- 短期均线高于长期均线时持有，否则空仓。
- 当日收盘产生信号，下一交易日开始计入持仓收益，避免未来数据。
- 绩效包括总收益、年化收益、基准收益、最大回撤、夏普比率和交易次数。

### 行情可靠性

- 数据通过 AKShare 接入。
- 历史行情按东方财富、腾讯、新浪的顺序自动回退。
- 网络数据不可用时可读取本地磁盘缓存。
- 页面显示本次实际使用的数据源。
- 已针对名称解析、数据回退、缓存和回测核心编写测试。
- 当前测试结果：`9 passed`。

### 协作与版本管理

- 代码已上传到公开仓库 `https://github.com/OverHabit/szy`。
- Windows 与 Mac 可通过 GitHub 拉取同一份代码和项目文档。
- Codex 获得授权后可按用户指令直接提交，不需要每次手工运行 PowerShell。
- 已增加 `CROSS_DEVICE_SETUP.md`，记录跨设备安装、Codex Git 权限、GitHub Token
  权限、安全边界和日常同步流程。
- 已加入 Render 免费 Web Service 的 `render.yaml` 部署清单：从 `master` 自动构建
  Streamlit 应用并在后续推送时自动更新。

## 已解决的问题

- 东方财富接口受代理影响时，不再让整个回测直接失败。
- 股票输入由仅支持代码扩展为代码或名称。
- Streamlit 首次启动的邮箱提示和本地服务未启动问题已明确处理。
- GitHub CLI 登录、远端历史合并和首次项目上传已经完成。

## 下一阶段路线

### P1：多策略框架

- 将策略抽象成统一接口和策略注册表，支持同一标的比较多种策略。
- 增加动量和均值回归策略。
- 支持同一股票并列比较多个策略的信号、收益与回撤。

### P2：股票池与图片识别

- 增加自选股列表的批量回测与多标的汇总。
- 支持粘贴多个股票代码或名称。
- 接入 OCR，从截图中提取股票名称和代码。
- 识别后先展示候选结果，由用户确认再加入股票池。

### P3：每日信号与通知

- 在 Hermes 创建本地工作日定时任务，在交易日 15:05 左右读取启用的自选策略。
- 对启用的自选策略运行并通知策略状态；后续可改为只发送新出现或发生变化的信号。
- 通知包含股票、策略、信号日期、参考收盘价、原因和风险提示。
- 保存任务运行记录，区分成功、数据未更新、无信号和执行失败。
- 15:05 至 15:30 的盘后固定价格交易只适用于符合交易所规则的证券和板块，
 不能把“15:30 前按收盘价成交”作为所有 A 股的统一规则。

### P4：云端部署

- 先使用 Render 免费 Web Service 托管研究网页；网页闲置 15 分钟后会休眠，首次
  再次访问约需一分钟唤醒。
- 后续如需长期运行定时任务、持久缓存和更稳定的访问，再迁移到阿里云或其他具备
  持久存储与定时任务的云服务。
- 使用持久化数据库保存股票池、策略配置、行情版本、信号和任务日志。
- 将访问密钥放入云端密钥管理或环境变量，不写入代码仓库。
- 增加登录、HTTPS、备份、监控和失败告警。

### P5：研究质量与风险控制

- 加入滑点、印花税、佣金下限、停牌和涨跌停成交约束。
- 支持仓位管理、组合回测和基准选择。
- 加入样本外验证、参数敏感性分析和防过拟合检查。
- 建立模拟盘观察期，再评估是否需要任何券商接口。

## 目标工作流

```text
行情源 -> 数据校验与缓存 -> 股票池 -> 多策略计算 -> 信号去重
       -> 网页展示 -> 通知 -> 用户确认 -> 人工交易

                    阿里云负责托管、定时任务、存储和监控
```

## 当前限制

- 自选信号数据库仅在本机持久化；云端 Streamlit 网页与运行 Hermes 的电脑不共享这份
  本地数据库。若要从公网网页修改后同步给 Hermes，后续需要接入受保护的共享数据库。
- 云端网页已具备 Render 部署清单，等待首次在 Render 控制台创建服务。
- OCR、批量回测仍未实现。Hermes 任务需由用户在客户端创建，代码已提供本地自选配置
  和脚本调用入口。
- 回测尚未完整模拟滑点、涨跌停、停牌、税费差异和实际成交价格。
- 免费行情接口可能限流、变更或暂时不可用，后续仍需持续监控和校验。

## 下一次开发建议

优先完成 P1 的多策略统一接口，同时保留现有双均线结果不变。完成后再增加股票池，
这样 OCR 和每日信号任务都可以复用同一套“股票池 + 策略列表”数据结构。
